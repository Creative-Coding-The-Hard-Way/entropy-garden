// @ts-check
import { defineConfig } from "astro/config";

// https://astro.build/config
export default defineConfig({
  site: "https://Creative-Coding-The-Hard-Way.github.io/entropy-garden",
  // The base path (repository name, starting with a forward slash)
  base: "/entropy-garden",
});
